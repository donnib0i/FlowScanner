services:
  - type: web
    name: flow-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 scanner_app.py
    plan: free
    envVars:
      - key: SCANNER_PIN
        sync: false
